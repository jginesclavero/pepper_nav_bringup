local_costmap:
    static_map: false
    rolling_window: true
    width: 2.0
    height: 2.0
    resolution: 0.07
    inflation_layer:
       inflation_radius: 0.30 # Default: 0.55 The radius in meters to which the map inflates obstacle cost values.
    static_map: 
       enabled: false   
