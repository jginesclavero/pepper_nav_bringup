local_costmap:
    global_frame: /map
    static_map: false
    rolling_window: true
    width: 2.5 # 1.5 (tobi)
    height: 2.5 # 1.5 (tobi)
    resolution: 0.05
    map_type: costmap
    cost_scaling_factor:  4.0
    inflation_radius:     0.45 # Default: 0.55 The radius in meters to which the map inflates obstacle cost values.
